---
title: 'JavaScript でローカルストレージに特定のキーを持つアイテムがあるかどうかを判定する'
date: 2025-03-17

tags: ['js', 'localstorage']
categories: [JavaScript]
---

JavaScript でローカルストレージに特定のキーを持つアイテムがあるかどうかを調べるには、 `in` operator を使う。

```js
console.log('theme' in localStorage)
// true or false
```

ここでは `theme` というアイテムがローカルストレージに保存されていれば `true` を返し、そうでなければ `false` を返す。

---

<Callout type="reference">
- [in - JavaScript | MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/in)

</Callout>
